#ifndef CORE_HPP
#define CORE_HPP
#include "geometry_algorithm.hpp"

using vec3f = vec3<>;
using vec3i = vec3<int>;
using vec3d = vec3<double>;

using line3f = Line<vec3<>>;
using line3d = Line<vec3<double>>;
using triangle3f = Triangle<vec3<>>;
using plane3f = Plane<vec3<>>;
#endif // CORE_HPP
